import { BreakpointWidthDirective } from './breakpoint-width.directive';

describe('BreakpointWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new BreakpointWidthDirective();
    expect(directive).toBeTruthy();
  });
});
